package treeDS;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class JUnitForTree {

	@Test
	void testToAddNode() {
		BinaryTree tree=new BinaryTree();
		TreeNode root=new TreeNode(3);
		 root.left = new TreeNode(2);
//	assertTrue(TreeNode root==3);
	}

}
